{
  "name": "server",
  "version": "1.0.0",
  "description": "test2",
  "author": "Ett Chung<ettoolong@gmail.com>",
  "private": true,
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 delete test2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
